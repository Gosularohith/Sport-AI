# Sports Quiz Agent

An AI-powered Sports Quiz Generator built with Streamlit, ChromaDB, and DuckDuckGo Web Search. 

## Setup
1. Replace the placeholder in `.env` with your OpenAI API Key.
2. Run `pip install -r requirements.txt`.
3. Run `streamlit run app.py` to start the app.
